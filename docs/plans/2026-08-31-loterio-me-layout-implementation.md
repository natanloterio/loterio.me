# loterio.me Layout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild loterio.me as a four-page static site that positions Natan Loterio for Senior/Staff Android roles with EU/UK relocation.

**Architecture:** Hand-written HTML with one shared stylesheet, served straight from GitHub Pages. No build step, no framework, no JavaScript shipped to the browser. A dependency-free Node script (`tools/check.mjs`) acts as the test suite: it asserts the spec's acceptance criteria against the files on disk, and it is written before the pages exist so the first run is red.

**Tech Stack:** HTML5, CSS3, IBM Plex Mono + IBM Plex Sans (Google Fonts). Dev-only: Node 26 (checker), `npx html-validate` (markup validation), `npx lighthouse` + headless Chrome (accessibility/SEO/best-practices), `python3 -m http.server` (local serving).

**Spec:** `docs/plans/2026-08-31-loterio-me-layout-design.md`

## Global Constraints

- **Zero JavaScript shipped.** The only permitted `<script>` tag is `type="application/ld+json"`. No `.js` file may be served.
- **No build step.** `tools/check.mjs` is a development tool; the site is the files on disk.
- **Every HTML file stays under 400 lines.**
- **One `<h1>` per page.** Heading levels descend without skipping.
- **All copy in English.** The target market is the EU/UK.
- **Every published number must be traceable** to the CV or to a measurement recorded in the spec. No estimates, no rounding up.
- **Contrast tokens are fixed** by the spec's table and must not be adjusted for taste without recalculating.
- **Colours are defined once**, in `assets/site.css`. No inline `style` attributes, no `<style>` blocks in page files.
- **Both colour schemes ship.** Light is the base; dark is a `prefers-color-scheme` override; print forces light.
- **External links** carry `rel="noopener noreferrer"` when they use `target="_blank"`.

## File Structure

| File | Responsibility |
|---|---|
| `index.html` | Home: hero, thesis, proof, repos, writing, track record, contact |
| `work/brownie.html` | Case study — on-device Android AI |
| `work/farfetch.html` | Case study — modernizing a live e-commerce app |
| `work/hugo-boss.html` | Case study — insourcing from an agency |
| `assets/site.css` | Every style rule for every page |
| `assets/cv.pdf` | CV download |
| `assets/natan.jpg` | Portrait, used in the hero and the Open Graph image |
| `assets/og.png` | Open Graph image, generated by headless Chrome |
| `tools/check.mjs` | Acceptance-criteria checker (dev only) |
| `sitemap.xml`, `robots.txt` | Discoverability |
| `CNAME` | Exists. Do not touch. |

The old `index.html` is replaced wholesale in Task 2. Its content is preserved in git history; nothing in it is reused.

## Author inputs

Four pieces of copy require knowledge that exists only with Natan. Each is a
real gap, not a placeholder to be filled with plausible text. Where a task
reaches one, it stops and asks. **Do not invent these.** Inventing a
reflection and attributing it to him is fabricating his professional
judgement, and an interviewer will find the seam.

| # | Input | Needed by |
|---|---|---|
| A1 | "What I'd do differently" — one paragraph per case study, three total | Tasks 3, 4, 5 |
| A2 | ~~How the FARFETCH pipeline went from 15 min to 2.5~~ — **answered**: modularization, then Gradle's build cache and configuration cache | resolved |
| A3 | Written permission to publish the HUGO BOSS figures | Task 5 |
| A4 | Whether to state EU citizenship or UK work authorization in the hero | Task 2 |
| A5 | ~~Confirmation that the thesis is true to how he sees his work~~ — **confirmed by Natan**; ship the drafted wording | resolved |

A1 is not optional and the section is never deleted. Spec §8 makes it a
required part of every case study, and the checker enforces at least 20 words
of real prose under that heading — so the build stays red, and the site does
not deploy, until Natan has written all three. That is the intended
behaviour: an empty section is a visible gap, an invented one is a lie with
his name on it, and a deleted one quietly drops the strongest seniority
signal on the site.

Never leave a guidance comment in the shipped HTML. HTML comments are visible
in view-source, and "Natan writes this paragraph" on a live job-search page is
worse than the empty section it marks. The checker fails on any such marker.

So the convention in Tasks 3–5 is: write the `<h2>What I'd do differently</h2>`
heading and put **nothing** under it. No paragraph, no comment, no placeholder
prose. The checker then reports `<page>: 0 words — author input A1
outstanding`, which names the gap precisely, keeps the build red, and leaks
nothing to a visitor who views source. When Natan supplies the paragraph, it
drops in under the heading and the criterion goes green.

If A3 is unanswered, Task 5 does not start.

---

### Task 1: Checker and stylesheet

Builds the test harness first, then the design tokens it will check. The
first checker run happens with no pages on disk, so it must fail loudly and
list exactly what the spec requires.

**Files:**
- Create: `tools/check.mjs`
- Create: `assets/site.css`

**Interfaces:**
- Consumes: nothing.
- Produces: `node tools/check.mjs` exits 0 when every criterion passes and 1
  otherwise, printing one line per criterion. Later tasks run it unchanged.
  `assets/site.css` exports the custom properties `--bg`, `--surface`,
  `--text`, `--muted`, `--accent`, `--rule`, `--mono`, `--sans`, `--measure`,
  `--page` and the spacing scale `--s1` … `--s16`.

- [ ] **Step 1: Write the checker**

Create `tools/check.mjs`:

```js
#!/usr/bin/env node
// Acceptance-criteria checker for loterio.me.
// Dev tool only — never served, never referenced by a page.
import { readFileSync, existsSync } from 'node:fs';

const PAGES = ['index.html', 'work/brownie.html', 'work/farfetch.html', 'work/hugo-boss.html'];
const CASES = PAGES.slice(1);
const CASE_SECTIONS = ['Problem', 'Constraint', 'Decisions', 'Result', "What I'd do differently"];
const TOKENS = ['--bg', '--surface', '--text', '--muted', '--accent', '--rule',
                '--mono', '--sans', '--measure', '--page'];

const results = [];
const check = (name, fn) => {
  let outcome;
  try { outcome = fn(); } catch (e) { outcome = e.message; }
  results.push({ name, ok: outcome === true, detail: outcome === true ? '' : outcome });
};
const read = (p) => existsSync(p) ? readFileSync(p, 'utf8') : null;
const textOf = (html) => html
  .replace(/<script[\s\S]*?<\/script>/g, ' ')
  .replace(/<style[\s\S]*?<\/style>/g, ' ')
  .replace(/<[^>]+>/g, ' ')
  .replace(/&[a-z]+;/g, ' ');
const words = (s) => s.split(/\s+/).filter(Boolean).length;

for (const page of PAGES) {
  check(`${page} exists`, () => read(page) !== null || 'missing');
}

check('no executable script tags', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) continue;
    for (const tag of html.match(/<script[^>]*>/g) || []) {
      if (!/type\s*=\s*["']application\/ld\+json["']/.test(tag)) bad.push(`${page}: ${tag}`);
    }
  }
  return bad.length === 0 || bad.join('; ');
});

check('every page under 400 lines', () => {
  const over = PAGES.filter(p => read(p) && read(p).split('\n').length >= 400);
  return over.length === 0 || `too long: ${over.join(', ')}`;
});

check('exactly one h1 per page', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) { bad.push(`${page}: missing`); continue; }
    const n = (html.match(/<h1[\s>]/g) || []).length;
    if (n !== 1) bad.push(`${page}: ${n}`);
  }
  return bad.length === 0 || bad.join('; ');
});

check('no skipped heading levels', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) continue;
    const levels = (html.match(/<h([1-6])[\s>]/g) || []).map(t => Number(t[2]));
    for (let i = 1; i < levels.length; i++) {
      if (levels[i] - levels[i - 1] > 1) bad.push(`${page}: h${levels[i - 1]} then h${levels[i]}`);
    }
  }
  return bad.length === 0 || bad.join('; ');
});

check('lang attribute on every page', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) { bad.push(page); continue; }
    if (!/<html[^>]+lang=/.test(html)) bad.push(page);
  }
  return bad.length === 0 || bad.join(', ');
});

check('every page has title, description and Open Graph tags', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) { bad.push(`${page}: missing`); continue; }
    for (const [label, re] of [
      ['title', /<title>[^<]{10,}<\/title>/],
      ['description', /<meta[^>]+name=["']description["'][^>]+content=(["'])(?:(?!\1).){40,}/],
      ['og:title', /property=["']og:title["'][^>]*content=(["'])(?:(?!\1).)+/],
      ['og:description', /property=["']og:description["'][^>]*content=(["'])(?:(?!\1).)+/],
      ['og:image', /property=["']og:image["'][^>]*content=(["'])(?:(?!\1).)+/],
    ]) if (!re.test(html)) bad.push(`${page}: ${label}`);
  }
  return bad.length === 0 || bad.join('; ');
});

check('no img without alt', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) continue;
    for (const tag of html.match(/<img[^>]*>/g) || []) if (!/\salt=/.test(tag)) bad.push(`${page}: ${tag}`);
  }
  return bad.length === 0 || bad.join('; ');
});

check('no inline styles', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) continue;
    if (/<style[\s>]/.test(html)) bad.push(`${page}: <style> block`);
    if (/\sstyle\s*=\s*["']/.test(html)) bad.push(`${page}: style attribute`);
  }
  return bad.length === 0 || bad.join('; ');
});

check('CV link precedes the first case-study link on the home page', () => {
  const html = read('index.html'); if (!html) return 'index.html missing';
  const cv = html.indexOf('assets/cv.pdf');
  const work = html.indexOf('work/');
  if (cv === -1) return 'no CV link';
  if (work === -1) return 'no case-study link';
  return cv < work || 'CV link appears after a case-study link';
});

check('home page states availability and relocation', () => {
  const html = read('index.html'); if (!html) return 'index.html missing';
  const t = textOf(html);
  return (/\bAvailable\b/.test(t) && /\brelocation\b/i.test(t)) || 'missing availability or relocation line';
});

check('home page carries schema.org Person JSON-LD', () => {
  const html = read('index.html'); if (!html) return 'index.html missing';
  const m = html.match(/<script type=["']application\/ld\+json["']>([\s\S]*?)<\/script>/);
  if (!m) return 'no JSON-LD block';
  const data = JSON.parse(m[1]);
  return data['@type'] === 'Person' || `@type is ${data['@type']}`;
});

check('case studies carry all five sections', () => {
  const bad = [];
  for (const page of CASES) {
    const html = read(page);
    if (!html) { bad.push(`${page}: missing`); continue; }
    const t = textOf(html);
    for (const s of CASE_SECTIONS) if (!t.includes(s)) bad.push(`${page}: ${s}`);
  }
  return bad.length === 0 || bad.join('; ');
});

check('case studies run 400-700 words', () => {
  const bad = [];
  for (const page of CASES) {
    const html = read(page);
    if (!html) { bad.push(`${page}: missing`); continue; }
    const body = html.match(/<main[\s\S]*?<\/main>/);
    const n = body ? words(textOf(body[0])) : 0;
    if (n < 400 || n > 700) bad.push(`${page}: ${n}`);
  }
  return bad.length === 0 || bad.join('; ');
});

check('every case study cites at least one number', () => {
  const bad = CASES.filter(p => !read(p) || !/\d/.test(textOf(read(p).match(/<main[\s\S]*?<\/main>/)?.[0] || '')));
  return bad.length === 0 || bad.join(', ');
});

check('stylesheet defines every token in both schemes', () => {
  const css = read('assets/site.css'); if (!css) return 'assets/site.css missing';
  const missing = TOKENS.filter(t => !css.includes(`${t}:`));
  if (missing.length) return `missing: ${missing.join(', ')}`;
  if (!css.includes('prefers-color-scheme: dark')) return 'no dark scheme';
  if (!css.includes('@media print')) return 'no print styles';
  return true;
});

check('every case study answers "what I\'d do differently"', () => {
  const bad = [];
  for (const page of CASES) {
    const html = read(page);
    if (!html) { bad.push(`${page}: missing`); continue; }
    const tail = html.split(/<h2[^>]*>\s*What I'd do differently\s*<\/h2>/)[1];
    if (tail === undefined) { bad.push(`${page}: no heading`); continue; }
    const n = words(textOf(tail.split('</main>')[0]));
    if (n < 20) bad.push(`${page}: ${n} words — author input A1 outstanding`);
  }
  return bad.length === 0 || bad.join('; ');
});

check('no author-input markers left in shipped HTML', () => {
  const bad = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) continue;
    for (const c of html.match(/<!--[\s\S]*?-->/g) || []) {
      if (/\bA[1-5]\b|Natan writes|Do not invent|TODO|TBD/i.test(c)) bad.push(`${page}: ${c.slice(0, 60)}`);
    }
  }
  return bad.length === 0 || bad.join('; ');
});

check('every page under 50KB', () => {
  const over = [];
  for (const page of PAGES) {
    const html = read(page); if (!html) continue;
    const kb = Buffer.byteLength(html, 'utf8') / 1024;
    if (kb > 50) over.push(`${page}: ${kb.toFixed(1)}KB`);
  }
  return over.length === 0 || over.join('; ');
});

check('CV, OG image, sitemap and robots.txt exist', () => {
  const missing = ['assets/cv.pdf', 'assets/og.png', 'sitemap.xml', 'robots.txt'].filter(f => !existsSync(f));
  return missing.length === 0 || `missing: ${missing.join(', ')}`;
});

check('sitemap lists every page', () => {
  const xml = read('sitemap.xml'); if (!xml) return 'sitemap.xml missing';
  const missing = PAGES.filter(p => !xml.includes(p === 'index.html' ? 'loterio.me/' : p));
  return missing.length === 0 || `missing: ${missing.join(', ')}`;
});

const failed = results.filter(r => !r.ok);
for (const r of results) console.log(`${r.ok ? 'PASS' : 'FAIL'}  ${r.name}${r.detail ? ` — ${r.detail}` : ''}`);
console.log(`\n${results.length - failed.length}/${results.length} passing`);
process.exit(failed.length ? 1 : 0);
```

- [ ] **Step 2: Run it and confirm it is red**

Run: `cd /media/loterio/workspace/workspace/personal/loterio.me && node tools/check.mjs`
Expected: exit code 1. The four `exists` lines report `missing`, and all
three case-study content criteria fail with `missing` rather than passing.

Criteria that assert the *absence* of something — no executable scripts, no
inline styles, no author-input markers left behind, page size and line count —
correctly pass on an empty tree; nothing shipped means nothing leaked. Any
criterion that asserts the *presence* of something and still reports PASS is
a bug in the checker, not progress. Fix it before continuing.

- [ ] **Step 3: Write the stylesheet**

Create `assets/site.css`. Light is the base scheme; dark overrides; print
forces light so a printed CV page does not waste ink. Contrast values come
from the spec's table and are not to be adjusted by eye.

```css
:root {
  --bg: #faf9f7;
  --surface: #f2f0ec;
  --text: #1a1c1a;
  --muted: #5a5f59;
  --accent: #157f4a;
  --rule: #d8d5cf;
  --mono: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
  --sans: 'IBM Plex Sans', system-ui, -apple-system, 'Segoe UI', sans-serif;
  --measure: 68ch;
  --page: 760px;
  --s1: 4px;  --s2: 8px;   --s3: 12px; --s4: 16px;
  --s6: 24px; --s8: 32px;  --s12: 48px; --s16: 64px;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0d0d0d;
    --surface: #161816;
    --text: #e6e6e3;
    --muted: #8b8f8a;
    --accent: #63c98a;
    --rule: #262826;
  }
}

*, *::before, *::after { box-sizing: border-box; }
body, h1, h2, h3, p, ul, ol, figure { margin: 0; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: var(--sans);
  font-size: 16px;
  line-height: 1.65;
  max-width: var(--page);
  margin: 0 auto;
  padding: var(--s16) var(--s6) var(--s12);
  -webkit-font-smoothing: antialiased;
}

a { color: var(--accent); text-underline-offset: 3px; }
a:hover { text-decoration-thickness: 2px; }
:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; border-radius: 2px; }

h1 { font-size: 32px; line-height: 1.2; letter-spacing: -0.02em; }
h2 {
  font-family: var(--mono);
  font-size: 13px;
  font-weight: 500;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--muted);
  padding-bottom: var(--s2);
  border-bottom: 1px solid var(--rule);
  margin-bottom: var(--s6);
}
h3 { font-size: 17px; line-height: 1.35; }
p { max-width: var(--measure); }
p + p { margin-top: var(--s4); }

.mono, .meta, .period, .stack { font-family: var(--mono); font-size: 14px; color: var(--muted); }

section { margin-top: var(--s16); }

/* Hero */
.hero h1 { margin-bottom: var(--s2); }
.hero .role { font-family: var(--mono); font-size: 14px; color: var(--muted); margin-bottom: var(--s4); }
.hero .status {
  font-size: 16px;
  border-left: 3px solid var(--accent);
  padding: var(--s1) 0 var(--s1) var(--s3);
  margin-bottom: var(--s6);
}
.actions { display: flex; flex-wrap: wrap; gap: var(--s2); }
.btn {
  font-family: var(--mono);
  font-size: 14px;
  text-decoration: none;
  color: var(--text);
  border: 1px solid var(--rule);
  border-radius: 3px;
  padding: 10px var(--s4);
  min-height: 44px;
  display: inline-flex;
  align-items: center;
}
.btn:hover { border-color: var(--accent); color: var(--accent); }
.btn.primary { border-color: var(--accent); color: var(--accent); font-weight: 500; }

/* Proof */
.proof-item { padding-top: var(--s6); border-top: 1px solid var(--rule); margin-top: var(--s6); }
.proof-item:first-of-type { border-top: 0; margin-top: 0; padding-top: 0; }
.proof-item h3 { margin-bottom: var(--s1); }
.proof-item dl { margin: var(--s3) 0 0; }
.proof-item dt {
  font-family: var(--mono); font-size: 12px; letter-spacing: 0.06em;
  text-transform: uppercase; color: var(--muted); margin-top: var(--s3);
}
.proof-item dd { margin: 2px 0 0; max-width: var(--measure); }

/* Repos and writing */
.repo-list, .plain { list-style: none; padding: 0; }
.repo-list li { padding: var(--s3) 0; border-bottom: 1px solid var(--rule); }
.repo-list .stack { margin-left: var(--s2); }

/* Track record */
.timeline { list-style: none; padding: 0; }
.timeline li {
  display: grid;
  grid-template-columns: 140px 1fr;
  gap: var(--s4);
  padding: var(--s3) 0;
  border-bottom: 1px solid var(--rule);
}
.timeline .period { font-size: 13px; }

/* Case studies */
.case-nav { font-family: var(--mono); font-size: 14px; margin-bottom: var(--s8); }
.case h2 { margin-top: var(--s12); }
.case p { margin-top: var(--s4); }
.case ul { max-width: var(--measure); padding-left: 1.1em; margin-top: var(--s4); }
.case li { margin-top: var(--s2); }

footer {
  margin-top: var(--s16);
  padding-top: var(--s6);
  border-top: 1px solid var(--rule);
  font-family: var(--mono);
  font-size: 13px;
  color: var(--muted);
}

@media (max-width: 640px) {
  body { padding: var(--s8) var(--s4) var(--s12); font-size: 15px; }
  h1 { font-size: 26px; }
  .timeline li { grid-template-columns: 1fr; gap: 2px; }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation: none !important; transition: none !important; }
}

@media print {
  :root {
    --bg: #ffffff; --surface: #ffffff; --text: #000000;
    --muted: #444444; --accent: #000000; --rule: #cccccc;
  }
  body { max-width: none; padding: 0; font-size: 11pt; }
  .actions, .case-nav { display: none; }
  a { text-decoration: underline; }
  a[href^="http"]::after { content: " (" attr(href) ")"; font-size: 9pt; color: #444; }
}
```

- [ ] **Step 4: Confirm the stylesheet criterion turns green**

Run: `node tools/check.mjs`
Expected: the line `stylesheet defines every token in both schemes` reads
PASS. Every page criterion still reads FAIL. Exit code still 1.

- [ ] **Step 5: Commit**

```bash
git add tools/check.mjs assets/site.css
git commit -m "test: add acceptance checker and design tokens"
```

---

### Task 2: Home page

**Files:**
- Modify: `index.html` (replaced wholesale)
- Modify: `assets/site.css` (hero portrait rules appended)
- Create: `assets/natan.jpg`

**Interfaces:**
- Consumes: `assets/site.css` classes `hero`, `role`, `status`, `actions`,
  `btn`, `proof-item`, `repo-list`, `timeline`, `plain` from Task 1.
- Produces: `work/brownie.html`, `work/farfetch.html` and `work/hugo-boss.html`
  are linked from the proof section; those files are created in Tasks 3–5 and
  the links are dead until then. That is expected mid-plan and is closed by
  Task 7.

- [ ] **Step 1: Ask about A4 and A5 before writing the hero**

A5 is resolved — Natan confirmed the thesis fits. Ship the drafted "What I
do" copy as written.

**A4**:

Ask Natan: does he hold EU citizenship, or existing UK work authorization?
If yes, one extra clause in the status line removes a recruiter's largest
unknown. If he does not answer, ship the line exactly as written below — it
states a preference and makes no legal claim, so it is correct either way.

- [ ] **Step 2: Generate the portrait**

Natan supplied a professional headshot. It is used twice: as a small avatar
in the hero, and inside the Open Graph card generated in Task 6 — a preview
card with a face outperforms a text-only one when the link is pasted into
LinkedIn, which is where this link will mostly be shared.

```bash
convert "/home/loterio/Pictures/Profile Pictures/Natan Pofile Picture.png" \
  -resize 400x400 -strip -interlace Plane -quality 82 assets/natan.jpg
```

Expected: roughly 18KB at 400×400. That single size serves the 96px hero
avatar at 4× and the Open Graph card at its larger size. Confirm with
`identify assets/natan.jpg`.

Keep it small and to the side. Natan is targeting the EU and UK: a portrait
is unremarkable in Portugal, Germany and the Netherlands, and less usual in
the UK, where some recruiters prefer to avoid photographs on bias grounds. A
96px avatar beside the name is warm without making the page a portfolio
template.

- [ ] **Step 3: Append the hero portrait rules to `assets/site.css`**

Add after the existing `.hero .status` rule. Do not modify any rule Task 1
wrote, and do not introduce new colour values — this is layout only.

```css
.hero { display: grid; grid-template-columns: 96px 1fr; gap: var(--s6); align-items: start; }
.hero > div { min-width: 0; }
.portrait { width: 96px; height: 96px; border-radius: 50%; display: block; }

@media (max-width: 640px) {
  .hero { grid-template-columns: 64px 1fr; gap: var(--s4); }
  .portrait { width: 64px; height: 64px; }
}
```

The portrait stays in its own column at every width rather than stacking
above the name on mobile. Stacking would push the availability line and the
CV button down, and acceptance criterion 1 requires both inside the first
390×844 frame.

- [ ] **Step 4: Write index.html**

Replace `index.html` entirely:

```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Natan Loterio — Senior / Staff Android Engineer</title>
<meta name="description" content="Senior / Staff Android Engineer with 15 years in Kotlin, Compose and on-device AI. Previously Tech Lead at HUGO BOSS and Android Engineer at FARFETCH. Open to relocation across the EU and UK.">
<meta property="og:title" content="Natan Loterio — Senior / Staff Android Engineer">
<meta property="og:description" content="Fifteen years of Android. I take systems shaped by someone else's constraints and put them back under the team's control.">
<meta property="og:image" content="https://loterio.me/assets/og.png">
<meta property="og:url" content="https://loterio.me/">
<meta property="og:type" content="profile">
<link rel="canonical" href="https://loterio.me/">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=IBM+Plex+Sans:wght@400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="assets/site.css">
<script type="application/ld+json">
{"@context":"https://schema.org","@type":"Person","name":"Natan Loterio","jobTitle":"Senior / Staff Android Engineer","email":"mailto:natanloterio@gmail.com","url":"https://loterio.me/","image":"https://loterio.me/assets/natan.jpg","address":{"@type":"PostalAddress","addressLocality":"Lisbon","addressCountry":"PT"},"alumniOf":{"@type":"CollegeOrUniversity","name":"FURB — Universidade de Blumenau"},"knowsAbout":["Android","Kotlin","Jetpack Compose","On-device machine learning","Mobile architecture"],"sameAs":["https://github.com/natanloterio","https://www.linkedin.com/in/natanloterio","https://natanloterio.substack.com"]}
</script>
</head>
<body>

<header class="hero">
  <img class="portrait" src="assets/natan.jpg" width="96" height="96" alt="Natan Loterio">
  <div>
    <h1>Natan Loterio</h1>
    <p class="role">Senior / Staff Android Engineer · Kotlin, Compose, on-device AI · 15 years</p>
    <p class="status">Available now. Open to relocation across the EU and UK.</p>
    <nav class="actions" aria-label="Profile links">
      <a class="btn primary" href="assets/cv.pdf">CV (PDF)</a>
      <a class="btn" href="https://github.com/natanloterio" rel="noopener noreferrer">GitHub</a>
      <a class="btn" href="https://www.linkedin.com/in/natanloterio" rel="noopener noreferrer">LinkedIn</a>
      <a class="btn" href="mailto:natanloterio@gmail.com">Email</a>
    </nav>
  </div>
</header>

<main>

<section id="thesis">
  <h2>What I do</h2>
  <p>I take systems shaped by someone else's constraints and put them back under the team's control. An Android app owned by an outside agency, brought in-house with a team built around it. A fifteen-minute pipeline cut to two and a half. A language model that only ran in someone else's cloud, running entirely on the phone instead.</p>
  <p>The work is usually the same shape: find what the team doesn't actually own, and change that. Fifteen years of it, most of them on Android.</p>
</section>

<section id="proof">
  <h2>Selected work</h2>

  <article class="proof-item">
    <h3>Brownie — on-device AI assistant for Android</h3>
    <p class="meta">Kotlin · Jetpack Compose · LiteRT-LM · open source</p>
    <dl>
      <dt>Constraint</dt>
      <dd>A capable tool-calling agent inside a phone's memory budget, with no cloud and no API key.</dd>
      <dt>Decision</dt>
      <dd>Gemma 4 E2B through LiteRT-LM with quantization chosen at runtime from device RAM, split across five Gradle modules so the inference layer can be swapped when the next on-device model lands.</dd>
      <dt>Result</dt>
      <dd>383 Kotlin files, multi-turn tool calling, persistent memory, MCP client. Nothing leaves the device.</dd>
    </dl>
    <p><a href="work/brownie.html">Read the case study</a> · <a href="https://github.com/natanloterio/Brownie" rel="noopener noreferrer">Source</a></p>
  </article>

  <article class="proof-item">
    <h3>HUGO BOSS — Tech Lead, Global Android Apps</h3>
    <p class="meta">2025–2026 · BOSS and HUGO · 15 countries, 8 languages</p>
    <dl>
      <dt>Constraint</dt>
      <dd>Move a live commerce app off an external agency without pausing delivery in fifteen markets.</dd>
      <dt>Decision</dt>
      <dd>Insource the codebase as it stood, build the internal Android team from zero, and write the conventions down so a new team could move without a senior engineer in every conversation.</dd>
      <dt>Result</dt>
      <dd>Development brought in-house; the conventions defined for Android were adopted across the whole mobile organization.</dd>
    </dl>
    <p><a href="work/hugo-boss.html">Read the case study</a> · <a href="https://play.google.com/store/apps/details?id=com.hugoboss.hugoboss" rel="noopener noreferrer">App</a></p>
  </article>

  <article class="proof-item">
    <h3>FARFETCH — Android Engineer</h3>
    <p class="meta">2021–2025 · luxury fashion marketplace</p>
    <dl>
      <dt>Constraint</dt>
      <dd>Modernize a production e-commerce app incrementally, with old and new patterns alive in the same codebase.</dd>
      <dt>Decision</dt>
      <dd>Jetpack Compose adopted screen by screen behind the existing navigation, inversion of control through dependency injection, and the build modularized so Gradle's build cache and configuration cache could turn most CI runs into partial builds.</dd>
      <dt>Result</dt>
      <dd>Pipeline from 15 minutes to 2.5. Compose and DI became the default for new work.</dd>
    </dl>
    <p><a href="work/farfetch.html">Read the case study</a> · <a href="https://play.google.com/store/apps/details?id=com.farfetch.farfetchshop" rel="noopener noreferrer">App</a></p>
  </article>

  <article class="proof-item">
    <h3>Pitaia — CTO</h3>
    <p class="meta">2026–present · AI-native commerce</p>
    <dl>
      <dt>Constraint</dt>
      <dd>Build a commerce platform where an autonomous agent, not a dashboard, is the product.</dd>
      <dt>Decision</dt>
      <dd>A 26-tool MCP layer exposing the whole product to language models, so the platform is fully operable by an agent.</dd>
      <dt>Result</dt>
      <dd>Blank repo to live stores with paying customers in under six months, including the first Instagram DM agent in the Brazilian creator market to close checkout — conversation to Pix or instalments — without the customer leaving the DM.</dd>
    </dl>
    <p><a href="https://pitaia.me" rel="noopener noreferrer">pitaia.me</a></p>
  </article>
</section>

<section id="open-source">
  <h2>Built in public</h2>
  <ul class="repo-list">
    <li><a href="https://github.com/natanloterio/LiveFolders" rel="noopener noreferrer">LiveFolders</a> — a virtual filesystem alternative to MCP: tools reach language models through file I/O.<span class="stack">Rust</span></li>
    <li><a href="https://github.com/natanloterio/scene-memory" rel="noopener noreferrer">scene-memory</a> — versioned-slot memory for conversational agents; scored 477/500 on LongMemEval.<span class="stack">Python</span></li>
    <li><a href="https://github.com/natanloterio/agent-team" rel="noopener noreferrer">agent-team</a> — a plugin that splits work across parallel coding agents and reviews their pull requests.<span class="stack">JavaScript</span></li>
    <li><a href="https://github.com/natanloterio/claude-code-employee-grade-skills" rel="noopener noreferrer">claude-code-employee-grade-skills</a> — skills extracted from Claude Code.</li>
    <li><a href="https://github.com/natanloterio/ArchitectTools" rel="noopener noreferrer">ArchitectTools</a> — IntelliJ plugin for architecture work.<span class="stack">Kotlin</span></li>
  </ul>
</section>

<section id="writing">
  <h2>Writing</h2>
  <ul class="plain">
    <li>
      <a href="https://natanloterio.substack.com/p/why-we-stopped-fighting-our-tools" rel="noopener noreferrer">Why we stopped fighting our tools</a>
      <span class="period">June 2026</span>
      <p>On adopting a tool that bends to how a team already works, instead of the other way round.</p>
    </li>
  </ul>
</section>

<section id="track-record">
  <h2>Track record</h2>
  <ul class="timeline">
    <li><span class="period">2026–present</span><span>CTO — Pitaia, Lisbon</span></li>
    <li><span class="period">2025–2026</span><span>Tech Lead, Global Android Apps — HUGO BOSS, Lisbon</span></li>
    <li><span class="period">2022–2026</span><span>LLM Engineer — GaiaHub, Lisbon <span class="period">(alongside FARFETCH)</span></span></li>
    <li><span class="period">2021–2025</span><span>Android Engineer — FARFETCH, Lisbon</span></li>
    <li><span class="period">2020–2021</span><span>Android Developer — Brelo, Florianópolis</span></li>
    <li><span class="period">2019–2020</span><span>Android Developer — Facio, São Paulo</span></li>
    <li><span class="period">2018–2019</span><span>Architecture Analyst — Cecred, Blumenau · drove R$5M in savings</span></li>
    <li><span class="period">2017–2018</span><span>Senior Android Developer — HBSIS, Blumenau</span></li>
    <li><span class="period">2014–2016</span><span>Senior Android Developer — ATAR and Morphy Digital Group</span></li>
    <li><span class="period">2012–2014</span><span>Android Developer — Microton Tecnologia, Blumenau</span></li>
    <li><span class="period">2008–2011</span><span>Delphi and ERP development</span></li>
  </ul>
  <p class="meta">BSc Computer Science, FURB — Universidade de Blumenau.</p>
</section>

<section id="contact">
  <h2>Contact</h2>
  <ul class="plain">
    <li><a href="mailto:natanloterio@gmail.com">natanloterio@gmail.com</a></li>
    <li><a href="https://www.linkedin.com/in/natanloterio" rel="noopener noreferrer">linkedin.com/in/natanloterio</a></li>
    <li><a href="https://github.com/natanloterio" rel="noopener noreferrer">github.com/natanloterio</a></li>
    <li><a href="assets/cv.pdf">Download CV (PDF)</a></li>
  </ul>
</section>

</main>

<footer>Lisbon, Portugal · Open to relocation across the EU and UK</footer>

</body>
</html>
```

- [ ] **Step 5: Run the checker**

Run: `node tools/check.mjs`
Expected to PASS: `index.html exists`, `no executable script tags`,
`no inline styles`, `no img without alt`, `CV link precedes the first
case-study link`, `home page states availability and relocation`, and
`home page carries schema.org Person JSON-LD`.

Expected to still FAIL: every criterion that iterates all four pages —
`exactly one h1 per page`, `lang attribute on every page`, and `every page
has title, description and Open Graph tags`. These cannot go green until
Tasks 3–5 create the three `work/*.html` files, because the Task 1 fix made
a missing page an honest failure rather than a silent skip. A green result
here would mean the checker regressed.

Case-study and asset criteria also still FAIL.

- [ ] **Step 6: Look at it**

Run: `python3 -m http.server 8000` then open `http://localhost:8000/`.
Confirm by eye: the hero, the availability line and the CV button are all
visible without scrolling on a 390×844 viewport, and the page is legible in
both light and dark system settings.

- [ ] **Step 7: Commit**

```bash
git add index.html assets/site.css assets/natan.jpg
git commit -m "feat: rebuild home page around thesis and evidence"
```

---

### Task 3: Brownie case study

The first case study also establishes the template that Tasks 4 and 5 copy.

**Files:**
- Create: `work/brownie.html`

**Interfaces:**
- Consumes: `assets/site.css` classes `case`, `case-nav` from Task 1.
- Produces: the case-study template. Tasks 4 and 5 copy this file and replace
  everything between `<main>` and `</main>`, plus the `<title>`, the meta
  description and the Open Graph tags.

- [ ] **Step 1: Ask for A1**

Ask Natan for one paragraph: building Brownie again from scratch, what would
he do differently? A wrong module boundary, a bet on LiteRT-LM he would
revisit, a feature that cost more than it returned. Do not write this
paragraph for him.

- [ ] **Step 2: Write work/brownie.html**

```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Brownie — on-device AI on Android · Natan Loterio</title>
<meta name="description" content="Building a tool-calling AI agent that runs entirely on an Android phone: Gemma 4 E2B through LiteRT-LM, quantization chosen from device RAM, five Gradle modules, no cloud.">
<meta property="og:title" content="Brownie — on-device AI on Android">
<meta property="og:description" content="A capable tool-calling agent inside a phone's memory budget. No cloud, no API key, no data leaving the device.">
<meta property="og:image" content="https://loterio.me/assets/og.png">
<meta property="og:url" content="https://loterio.me/work/brownie.html">
<link rel="canonical" href="https://loterio.me/work/brownie.html">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=IBM+Plex+Sans:wght@400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="../assets/site.css">
</head>
<body>

<nav class="case-nav"><a href="../index.html">&larr; Natan Loterio</a></nav>

<main class="case">
<h1>Brownie — on-device AI on Android</h1>
<p class="meta">Personal project · Kotlin, Jetpack Compose, LiteRT-LM · <a href="https://github.com/natanloterio/Brownie" rel="noopener noreferrer">source on GitHub</a></p>

<h2>Problem</h2>
<p>Every capable AI assistant on a phone is a thin client. The model lives in someone else's datacentre, the conversation travels there and back, and the whole thing stops working on a plane or behind a corporate firewall. Privacy is a policy promise rather than a property of the system.</p>
<p>I wanted to know what was actually left if you refused all of that — not a chatbot demo, but a real agent that calls tools, reads and writes files, remembers across sessions and drives the device, running entirely on the hardware in your pocket.</p>

<h2>Constraint</h2>
<p>A phone is not a server. There is no meaningful swap, the memory budget is shared with every other running app, and sustained inference runs straight into thermal throttling. The model weights, the tool layer, the conversation history and the UI all have to fit in what a mid-range Android device can spare — and the interface has to stay responsive while the model is working.</p>
<p>The second constraint was self-imposed and harder: no cloud fallback. A hybrid design that quietly calls a remote model when the device struggles would have made every demo smoother and destroyed the reason for building it.</p>

<h2>Decisions</h2>
<ul>
<li><strong>Quantization chosen at runtime, not at build time.</strong> The app detects available RAM on first launch and picks a tier: Q4 at roughly 1.5&nbsp;GB for 6–8&nbsp;GB devices, Q4 plus a vision projector for 8–12&nbsp;GB, Q8 with vision above that. Below 6&nbsp;GB the app says it will not work, rather than failing slowly and blaming the user.</li>
<li><strong>Five Gradle modules instead of one.</strong> <code>:core:inference</code> wraps LiteRT-LM, model download and device detection; <code>:core:agent</code> holds the loop; <code>:core:tools</code> the registry and its twelve implementations; <code>:core:memory</code> persistence, profile and full-text session search; <code>:app</code> the Compose UI. The boundary that earned its keep was inference — it is the piece most certain to be replaced when the next on-device model ships.</li>
<li><strong>Context compression rather than truncation.</strong> When the window fills, the agent summarises and continues. Truncation would have been a day's work; losing the earlier half of a conversation is exactly the failure users cannot diagnose.</li>
<li><strong>An MCP client.</strong> New tools can be added by pointing the app at a server, without shipping an APK.</li>
</ul>

<h2>Result</h2>
<p>383 Kotlin files across five modules, 198 commits. Multi-turn tool calling up to ten iterations per message, streaming responses with tool-call chips in the UI, file tools in a sandboxed workspace, contacts, calendar, clipboard, scheduled autonomous tasks, screen reading and device automation through the accessibility APIs, web extraction through a WebView, and speech in both directions.</p>
<p>No cloud, no API key, no data leaving the phone. After the one-time model download, the engine loads in five to fifteen seconds and the app works with the network off.</p>

<h2>What I'd do differently</h2>
</main>

<footer>Natan Loterio · <a href="../index.html">loterio.me</a></footer>

</body>
</html>
```

- [ ] **Step 3: Run the checker**

Run: `node tools/check.mjs`
Expected: `work/brownie.html exists` PASS. The word-count criterion reports
brownie's count — confirm it lands between 400 and 700. If A1 is still
missing, `case studies carry all five sections` will pass on the heading
alone while the paragraph is empty; that is a known gap tracked as A1, not a
green light.

- [ ] **Step 4: Commit**

```bash
git add work/brownie.html
git commit -m "feat: add Brownie case study"
```

---

### Task 4: FARFETCH case study

**Files:**
- Create: `work/farfetch.html`

**Interfaces:**
- Consumes: the template established by `work/brownie.html` in Task 3.
- Produces: nothing later tasks depend on.

- [ ] **Step 1: Ask for A1**

A2 is answered and already written into the copy below: modularization
first, then Gradle's build cache and configuration cache. Do not reword the
mechanism. The order is the substance of the answer, and the two caches are
named separately on purpose because they remove different costs — an Android
interviewer will probe exactly this.

Ask **A1**: what would Natan do differently about the Compose migration?

- [ ] **Step 2: Write work/farfetch.html**

Copy `work/brownie.html`, then replace the `<title>`, meta description,
`og:title`, `og:description`, `og:url` and `canonical` with the FARFETCH
values below, and replace everything between `<main class="case">` and
`</main>` with this content:

```html
<h1>FARFETCH — modernizing a live e-commerce app</h1>
<p class="meta">Android Engineer · August 2021 – April 2025 · <a href="https://play.google.com/store/apps/details?id=com.farfetch.farfetchshop" rel="noopener noreferrer">FARFETCH on Google Play</a></p>

<h2>Problem</h2>
<p>FARFETCH is a luxury fashion marketplace, and its Android app takes orders from customers around the world. By 2021 the codebase carried years of accumulated decisions: MVC in the oldest screens, MVVM in the newer ones, dependencies wired by hand throughout. None of it was wrong when it was written, and all of it made the next change slower than the last.</p>
<p>The continuous integration pipeline took fifteen minutes per run. Fifteen minutes is past the threshold where engineers stop running it and start batching, which is how integration problems get found late and expensively.</p>

<h2>Constraint</h2>
<p>Nothing could stop. The app was in production taking revenue, on a release train that did not pause for architecture work. Every change had to land incrementally, and both the old and the new patterns had to stay alive in the same codebase for as long as the migration ran — which meant years, not sprints.</p>

<h2>Decisions</h2>
<ul>
<li><strong>Jetpack Compose adopted screen by screen, behind the existing navigation.</strong> New screens were written in Compose; old ones moved when they were being opened for other reasons anyway. No migration sprint, no feature freeze, no branch that lives for six months.</li>
<li><strong>Inversion of control through dependency injection.</strong> This was the change that made the rest possible. Untangling construction from behaviour is what let a single screen move without dragging its dependencies with it.</li>
<li><strong>Modularization first, caching second — in that order.</strong> Splitting the monolith into Gradle modules is what made the build cacheable at all. With a single module almost any change invalidates the whole build, so a cache buys you very little; once the boundaries exist, a change touches a few modules and the rest are served from cache.</li>
<li><strong>Then both Gradle caches, against two different costs.</strong> The build cache reuses task outputs across builds and across machines, so CI stops recomputing what another run already produced. The configuration cache removes the configuration phase from repeat runs — the fixed price every single build paid before, regardless of how little had changed.</li>
</ul>

<h2>Result</h2>
<p>The pipeline went from 15 minutes to 2.5 on average — back under the threshold where engineers run it per change instead of batching.</p>
<p>Compose and dependency injection both became the default for new work rather than parallel experiments, which is the only durable outcome an incremental migration can have: the new way has to be the easy way, or the old way wins by inertia.</p>

<h2>What I'd do differently</h2>
```

Head values for this page:

```html
<title>FARFETCH — modernizing a live e-commerce app · Natan Loterio</title>
<meta name="description" content="Modernizing FARFETCH's Android app in production: incremental Jetpack Compose adoption, dependency injection, and a CI pipeline cut from 15 minutes to 2.5.">
<meta property="og:title" content="FARFETCH — modernizing a live e-commerce app">
<meta property="og:description" content="Incremental Compose adoption and dependency injection on a production e-commerce app, plus a pipeline cut from 15 minutes to 2.5.">
<meta property="og:url" content="https://loterio.me/work/farfetch.html">
<link rel="canonical" href="https://loterio.me/work/farfetch.html">
```

- [ ] **Step 3: Run the checker**

Run: `node tools/check.mjs`
Expected: `work/farfetch.html exists` PASS, word count within 400–700, and
`every case study cites at least one number` PASS.

- [ ] **Step 4: Commit**

```bash
git add work/farfetch.html
git commit -m "feat: add FARFETCH case study"
```

---

### Task 5: HUGO BOSS case study

**Blocked on A3.** Do not start this task until Natan has confirmed the
figures may be published. The numbers appear on his public LinkedIn profile,
which is a strong argument that republishing them creates no new exposure —
but that is reasoning, not permission, and he is the one carrying the
consequence.

**Files:**
- Create: `work/hugo-boss.html`

**Interfaces:**
- Consumes: the template established by `work/brownie.html` in Task 3.
- Produces: nothing later tasks depend on.

- [ ] **Step 1: Confirm A3, then ask for A1**

Confirm publication permission for: fifteen countries, eight languages,
500k+ installs, 4.5-star rating, and the fact that development was insourced
from an external agency. If any single figure is withheld, drop that figure
and keep the rest — the narrative survives without any one number.

Then ask A1: what would he do differently about the insourcing?

- [ ] **Step 2: Write work/hugo-boss.html**

Copy `work/brownie.html`, replace the head values as below, and replace
everything between `<main class="case">` and `</main>` with:

```html
<h1>HUGO BOSS — bringing global Android apps in-house</h1>
<p class="meta">Tech Lead, Global Android Apps · March 2025 – February 2026 · <a href="https://play.google.com/store/apps/details?id=com.hugoboss.hugoboss" rel="noopener noreferrer">HUGO BOSS on Google Play</a></p>

<h2>Problem</h2>
<p>The BOSS and HUGO apps ship to customers in fifteen countries and eight languages, with more than 500,000 installs and a 4.5-star rating on Google Play. They carry e-commerce, click-and-collect, in-store appointments and the HUGO BOSS XP loyalty programme.</p>
<p>They were built and operated by an external agency. That arrangement works until the app becomes central to the business, and then every question about it — what gets built next, how quickly, what the code looks like underneath — has to travel through a contract before it can be answered.</p>

<h2>Constraint</h2>
<p>The apps were live commerce in fifteen markets throughout the transition. Bringing development in-house could not mean a pause, a rewrite, or a quarter of degraded delivery. The handover had to happen underneath a running business, and the team receiving the codebase did not exist yet.</p>

<h2>Decisions</h2>
<ul>
<li><strong>Insource first, improve second.</strong> Take ownership of the codebase exactly as it stood rather than treating the handover as licence for a rewrite. A rewrite would have been the most satisfying decision available and the one most likely to end the programme early.</li>
<li><strong>Build the internal Android team from zero</strong> — hiring, onboarding, and the working agreements a brand-new team needs before it can move at all.</li>
<li><strong>Write the conventions down.</strong> Coding standards, architecture patterns, workflows, way of working. With a new team and an inherited codebase, tacit knowledge is not available to lean on: making the conventions explicit is what let people make correct decisions without a senior engineer present for each one.</li>
<li><strong>Modernize under delivery, not instead of it</strong>, so the app could keep absorbing e-commerce, click-and-collect, in-store appointments and loyalty features while the base was being brought up to current standards.</li>
</ul>

<h2>Result</h2>
<p>Development moved from agency delivery to an internal team, on apps that stayed live in fifteen markets throughout.</p>
<p>The clearest signal that the work outlived the project: the conventions, architecture patterns and way of working defined for Android were adopted by management across the whole mobile organization. The codebase was modernized to current engineering standards so that e-commerce, click-and-collect, in-store appointments and the HUGO BOSS XP loyalty programme could scale with the business rather than against it.</p>

<h2>What I'd do differently</h2>
```

Head values for this page:

```html
<title>HUGO BOSS — bringing global Android apps in-house · Natan Loterio</title>
<meta name="description" content="Insourcing the BOSS and HUGO Android apps from an external agency across 15 markets, building the internal team from zero, and setting conventions the whole mobile organization adopted.">
<meta property="og:title" content="HUGO BOSS — bringing global Android apps in-house">
<meta property="og:description" content="Moving live commerce apps off an agency in 15 countries, and building the internal Android team that took them over.">
<meta property="og:url" content="https://loterio.me/work/hugo-boss.html">
<link rel="canonical" href="https://loterio.me/work/hugo-boss.html">
```

- [ ] **Step 3: Run the checker**

Run: `node tools/check.mjs`
Expected: all four `exists` criteria PASS; every case-study criterion PASS.

- [ ] **Step 4: Commit**

```bash
git add work/hugo-boss.html
git commit -m "feat: add HUGO BOSS case study"
```

---

### Task 6: Assets and discoverability

**Files:**
- Create: `assets/cv.pdf`, `assets/og.png`, `sitemap.xml`, `robots.txt`
- Create then delete: `assets/og-src.html`
- Consumes: `assets/natan.jpg` from Task 2

**Interfaces:**
- Consumes: `assets/site.css` tokens from Task 1.
- Produces: the four files the checker's asset criteria look for.

- [ ] **Step 1: Copy the CV in**

```bash
cp /home/loterio/Downloads/Profile-8.pdf assets/cv.pdf
```

The source is a LinkedIn profile export. Ask Natan whether he would rather
ship a purpose-built CV — a LinkedIn export is recognisable as one, and for a
Staff application a tailored two-page document reads better. Shipping the
export now is correct; it beats an empty link, and it can be replaced without
touching any other file.

- [ ] **Step 2: Generate the Open Graph image**

The card puts the portrait beside the text. Write it into `assets/` rather
than `/tmp` so the relative `natan.jpg` reference resolves; delete it after.

Write `assets/og-src.html`:

```html
<!doctype html>
<html><head><meta charset="utf-8">
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=IBM+Plex+Sans:wght@500&display=swap" rel="stylesheet">
<style>
body { margin:0; width:1200px; height:630px; background:#0d0d0d; color:#e6e6e3;
       font-family:'IBM Plex Sans',sans-serif; display:flex; align-items:center;
       gap:80px; padding:0 90px; box-sizing:border-box; }
h1 { font-size:76px; margin:0 0 18px; letter-spacing:-0.02em; }
p  { font-family:'IBM Plex Mono',monospace; font-size:30px; color:#8b8f8a; margin:0; }
.a { color:#63c98a; margin-top:40px; font-family:'IBM Plex Mono',monospace; font-size:26px; }
img { width:300px; height:300px; border-radius:50%; flex:none; }
</style></head>
<body>
<div>
<h1>Natan Loterio</h1>
<p>Senior / Staff Android Engineer</p>
<p class="a">Kotlin · Compose · on-device AI · 15 years</p>
</div>
<img src="natan.jpg" alt="">
</body></html>
```

Then:

```bash
google-chrome --headless --disable-gpu --screenshot=assets/og.png \
  --window-size=1200,630 --hide-scrollbars --virtual-time-budget=3000 \
  assets/og-src.html
rm assets/og-src.html
```

Open `assets/og.png` and confirm the fonts rendered rather than falling back
to a default sans — headless Chrome sometimes screenshots before webfonts
load. If they did fall back, re-run the command; if it persists, add
`--virtual-time-budget=3000`.

- [ ] **Step 3: Write sitemap.xml**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>https://loterio.me/</loc><priority>1.0</priority></url>
  <url><loc>https://loterio.me/work/brownie.html</loc><priority>0.8</priority></url>
  <url><loc>https://loterio.me/work/hugo-boss.html</loc><priority>0.8</priority></url>
  <url><loc>https://loterio.me/work/farfetch.html</loc><priority>0.8</priority></url>
</urlset>
```

- [ ] **Step 4: Write robots.txt**

```
User-agent: *
Allow: /

Sitemap: https://loterio.me/sitemap.xml
```

- [ ] **Step 5: Run the checker**

Run: `node tools/check.mjs`
Expected: `CV, OG image, sitemap and robots.txt exist` PASS and `sitemap
lists every page` PASS. Exit code 0, assuming Tasks 2–5 are complete.

- [ ] **Step 6: Commit**

```bash
git add assets/cv.pdf assets/og.png sitemap.xml robots.txt
git commit -m "feat: add CV, Open Graph image, sitemap and robots.txt"
```

---

### Task 7: Full verification

Everything above proves the files satisfy the checker. This task proves they
satisfy a browser, a validator and an auditor.

**Files:**
- Modify: whichever files the audits find fault with.

**Interfaces:**
- Consumes: every file from Tasks 1–6.
- Produces: a site that meets all nine acceptance criteria in the spec.

- [ ] **Step 1: Validate the markup**

```bash
npx -y html-validate index.html work/*.html
```

Expected: no errors. Fix any that appear; do not silence rules to get past
them.

- [ ] **Step 2: Audit with Lighthouse**

```bash
python3 -m http.server 8000 &
SERVER=$!
npx -y lighthouse http://localhost:8000/ \
  --only-categories=accessibility,seo,best-practices \
  --chrome-flags="--headless --no-sandbox" \
  --output=json --output-path=/tmp/lh-home.json --quiet
npx -y lighthouse http://localhost:8000/work/brownie.html \
  --only-categories=accessibility,seo,best-practices \
  --chrome-flags="--headless --no-sandbox" \
  --output=json --output-path=/tmp/lh-case.json --quiet
kill $SERVER
node -e "for (const f of ['/tmp/lh-home.json','/tmp/lh-case.json']) {
  const r = require(f);
  console.log(f, Object.entries(r.categories).map(([k,v]) => k+': '+v.score*100).join('  '));
}"
```

Expected: 100 in all three categories on both pages. Anything below 100 is a
defect to fix, not a score to accept.

- [ ] **Step 3: Check the mobile fold**

```bash
python3 -m http.server 8000 &
SERVER=$!
google-chrome --headless --disable-gpu --screenshot=/tmp/fold.png \
  --window-size=390,844 --hide-scrollbars http://localhost:8000/
kill $SERVER
```

Open `/tmp/fold.png`. Acceptance criterion 1 requires the name, the target
role, the availability line, the relocation line and the CV button all to be
visible in that frame. If any is cut off, reduce the hero's vertical spacing
until they fit — this criterion outranks the spacing scale.

- [ ] **Step 4: Check the page without CSS and on paper**

Open `http://localhost:8000/` in Chrome, then in DevTools disable
`assets/site.css` and reload. The page must still read top to bottom in a
sensible order with a clear heading hierarchy. Then open the print preview
and confirm black-on-white output with link URLs expanded in the margin.

- [ ] **Step 5: Verify every external link**

```bash
grep -ho 'https://[^"]*' index.html work/*.html | sort -u | while read u; do
  printf '%s  %s\n' "$(curl -s -o /dev/null -w '%{http_code}' -L --max-time 15 -A 'Mozilla/5.0' "$u")" "$u"
done
```

Expected: every URL returns 200, with one expected exception before the first
deploy: `https://loterio.me/assets/og.png` and any other `loterio.me` URL
return 404 until Step 7 has run. Re-run this check after deploying and
confirm they turn 200. Every other dead link is a defect — a broken link on a
job-search site costs more than anything else on the page.

- [ ] **Step 6: Final checker run and commit**

```bash
node tools/check.mjs
git add -A
git commit -m "fix: address validation, accessibility and layout findings"
```

Expected: exit code 0, every criterion PASS.

- [ ] **Step 7: Confirm the deploy**

Push to `master` and wait for GitHub Pages. Then verify the live site:

```bash
curl -s -o /dev/null -w '%{http_code}\n' https://loterio.me/
curl -s https://loterio.me/ | grep -c 'Available now'
```

Expected: 200, and the availability line present in the served HTML.

---

## Housekeeping (optional, outside the plan)

Two leftovers from the site's earlier React incarnation, neither of which
affects the work above:

- `node_modules/` holds `react`, `react-dom`, `react-scripts` and
  `@testing-library`. It is gitignored, so it pollutes nothing but the local
  disk. Safe to delete.
- `origin` carries 21 stale `dependabot/npm_and_yarn/*` branches for those
  dependencies. Deleting remote branches is Natan's call, not the plan's.
